require "spec_helper"

describe MyAddress do
  it "has a version number" do
    expect(MyAddress::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
